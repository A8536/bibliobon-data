# Manual Data

Use this directory for curated data that should survive rebuilds:

- author merge rules;
- journal and issue correction rules;
- work/container merge rules;
- tag and section overrides;
- manual controlled vocabularies;
- explicit exclusions.

Generated data in `data/` should be recreated from source inputs plus these manual files.
